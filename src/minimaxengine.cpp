#include "othengine.h"
#include <stdlib.h>
#include <list>

namespace oth {
    class MinimaxEngine : public Engine {

        Point nextMove(const Othello& board, Color color) const {
            
            return Point(0, 0);
        }
    };
}